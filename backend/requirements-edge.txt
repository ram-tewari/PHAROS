# Pharos Edge Requirements
# Edge-specific dependencies (with GPU support)

-r requirements-base.txt

# PyTorch with CUDA 11.8 support (RTX 4070 / consumer GPUs)
# Pull CUDA 11.8 compiled wheels — do NOT install vanilla torch from PyPI
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.7.1+cu118
torchvision==0.22.1+cu118
torchaudio==2.7.1+cu118

# Transformers with GPU support
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For quantization

# Sentence Transformers (with GPU support, nomic-ai models require >=2.3.0)
sentence-transformers>=2.3.0

# Required by nomic-ai/nomic-embed-text-v1 custom model code
einops>=0.7.0

# FAISS for local vector search
faiss-cpu>=1.7.0
# faiss-gpu>=1.7.0  # Uncomment for GPU support

# Qdrant (local vector database)
qdrant-client>=1.7.0

# Additional ML utilities
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# pharos-edge CLI (status rendering)
rich>=13.0
safetensors>=0.4.0

# ── Polyglot Tree-Sitter grammars (Phase 2 AST parser) ────────────────
# Pinned explicitly here even though requirements-base.txt lists them —
# edge venvs created before the Phase 2 cutover need a forced re-resolve
# or language_parser.py falls back to naive block-chunking and logs:
#   "Grammar 'go' requires package 'tree_sitter_go'. Install with: ..."
tree-sitter>=0.23.0
tree-sitter-c>=0.24.0
tree-sitter-cpp>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-rust>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-python>=0.23.0